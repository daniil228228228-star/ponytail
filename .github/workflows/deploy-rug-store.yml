name: Deploy rug-store to GitHub Pages

on:
  push:
    branches:
      - claude/designer-uqt6tn
    paths:
      - 'rug-store/**'
      - '.github/workflows/deploy-rug-store.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: rug-store/package-lock.json
      - working-directory: rug-store
        run: npm ci
      - working-directory: rug-store
        run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: rug-store/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
